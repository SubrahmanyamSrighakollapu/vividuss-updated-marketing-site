# Set this before building for production canonicals and sitemap URLs.
NEXT_PUBLIC_SITE_URL=https://www.vividuss.com
# Optional existing hosted form endpoint (HTTPS only). Empty uses an email draft.
NEXT_PUBLIC_CONTACT_FORM_ENDPOINT=
# Optional hosted newsletter endpoint. Empty prepares an email subscription request.
NEXT_PUBLIC_NEWSLETTER_FORM_ENDPOINT=
